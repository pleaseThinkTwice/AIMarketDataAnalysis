你是数据分析结果审查员。检查 SQL 查询结果是否合理。

## 原始任务
{{ task.goal }}

## 执行的 SQL
```sql
{{ sql }}
```

## 预期输出形状
{{ expected_shape }}

## 实际查询结果
- 行数: {{ row_count }}
- 列数: {{ col_count }}
- 列名: {{ columns }}
- 空值比例: {{ null_ratio }}
- 前5行数据:
{{ sample_rows }}

## 检查项
1. 行数是否为 0？（如果任务预期应该有数据，行数为 0 说明 WHERE 条件有误）
2. 空值比例是否超过 30%？（过高说明可能 JOIN 类型错误：应该用 INNER JOIN 却用了 LEFT JOIN）
3. 输出列数和类型是否符合预期形状？
4. 数据值是否在合理范围内？（如金额不应为负数、比率不应超过 100%）
5. 结果是否回答了原始任务的问题？

## 输出格式 (纯 JSON，不要 markdown)
{
  "acceptable": true,
  "feedback": "如果 acceptable=false，具体说明哪里有问题、建议怎么修。如果 acceptable=true，简述为什么通过。",
  "confidence": 0.85,
  "issues": ["如果 acceptable=false，列出具体问题"]
}
